namespace ClassNotes.API.Constants
{
    public static class ScoreTypeConstant
    {
        public const string GOLD_SCORE = nameof(GOLD_SCORE);
        public const string ARITHMETIC_SCORE = nameof(ARITHMETIC_SCORE);
        public const string WEIGHTED_SCORE = nameof(WEIGHTED_SCORE);
    }
}